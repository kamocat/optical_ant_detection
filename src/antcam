#!/usr/bin/env ash
# Entry point — orchestrates capture → notify.
# Env vars (MQTT_*, NTFY_*, CAM_URL) are picked up by the child scripts.

SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"

COUNT=$("$SCRIPT_DIR/capture")
echo "$COUNT"
"$SCRIPT_DIR/notify" "$COUNT"